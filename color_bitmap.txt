--pseudocode--

color_bitmap()
# initialise
	t0 = 0( rij)
	t1 = 0 (kolom)
	
loop:
	t2 = charcount (readbuffer)
	
	readbuffer(charcount)
		# bepaalt telkens het volgende char in de buffer
		output: char
		
	color_pixel(char, row, col)
		# bepaal eerst de kleur op basis van char
		kleur = ...
		
		bereken_addr(row, col)
			output: addr # hier zal de kleur geschreven worden
			
	# schrijf de kleur naar addr
			
	
	
	nextbit:
		if rij<31: rij++ # increment
		elif rij==31: rij=0; kolom++
		charcount++
		j loop
		
		
		